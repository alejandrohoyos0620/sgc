import { ToSpanishPipe } from './to-spanish.pipe';

describe('ToSpanishPipe', () => {
  it('create an instance', () => {
    const pipe = new ToSpanishPipe();
    expect(pipe).toBeTruthy();
  });
});
